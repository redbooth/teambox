.text_field.name
  = f.label :name, t('.name')
  = f.text_field :name
  = tooltip 'user_description', t('.name') if conversation.new_record?

- if conversation.new_record?
  .text_area.body
    = f.label :body, t('.body')
    = f.text_area :body
    = tooltip 'user_body', t('.body')
