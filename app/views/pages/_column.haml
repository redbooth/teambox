- content_for :column do
  - if project
    - if project.editable?(current_user)
      = new_page_link(project)
    - else
      %p= t('.cant_create')
  .pages
    %h2= t('.all_pages')
    = list_pages(pages,current_target)
