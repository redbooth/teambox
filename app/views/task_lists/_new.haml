- task_list_form_for(project,task_list) do |f|
  = task_list_fields(f)
  = task_list_submit(project,task_list)

  .submit
    = f.submit t('.submit')
    = t('common.or')
    = link_to_function t('form.cancel'), hide_new_task_list
    
