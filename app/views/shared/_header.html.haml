.header
  - logo = @organization ? @organization.logo(:top) : logo_image
  %h1= link_to image_tag(logo, :alt => "Teambox"), root_path
  
  - unless request.format == :m
    = render 'shared/global_navigation'

  = search_bar
