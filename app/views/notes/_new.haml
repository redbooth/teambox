- form_for [project,page,note], :html => { :id => "new_note_form", :style => 'display: none' } do |f|
  = page_slot_fields("new_note_form")
  = note_fields(f)
  .submit{ :id => "note_submit" }
    = f.submit t('.submit')
    or
    = link_to t('common.cancel'), '#', :class => 'cancelPageWidget'
  =loading('note_form')