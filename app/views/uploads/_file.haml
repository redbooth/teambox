.upload_file{:id => "upload_#{upload.id}"}
  = file_icon_image(upload,'16px')
  %span.filename
    = link_to_upload(upload, upload.file_name)
  %span.size
    = parenthesize(number_to_human_size(upload.size))