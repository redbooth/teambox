%h2= t('.title')

-form_tag session_path, :class => 'new_session' do
  = session_fields
  .submit
  = submit_tag t('.submit')

.banner_wrap
  .banner
    %h2
      Don't have an Account?
      
.clear