= loading_image(task_id('delete_loading',project,task_list,task))
.text_actions{ :id => task_id('edit_actions',project,task_list,task) }
  = task_link(project,task_list,task)
  - if task.editable?(current_user)
    = t('common.or')
    = delete_task_link(project,task_list,task)