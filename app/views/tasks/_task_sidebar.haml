.task{ :class => task_classes(task) }
  %span.taskStatus
    = task_status(task,:content)
  = render_due_on(task,current_user)
  = link_to h(task.name), project_task_list_task_path(task.project,task.task_list,task), :class => "name"