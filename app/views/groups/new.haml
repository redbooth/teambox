%h2= t('.title')

%p= t('.explain')

- form_for @group, :html => { :multipart => true }  do |f|
  = f.error_messages
  = render 'groups/fields', :f => f
  = group_logo_fields(@group, f)
  .submit
    = f.submit t('.submit')

= groups_column