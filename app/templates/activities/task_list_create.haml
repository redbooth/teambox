.activity
  {{#user}}
  <a href="{{task_list_url}}" class="micro_avatar" style="background: url({{micro_avatar_url}}) no-repeat"></a>
  {{/user}}
  .activity_block
    .date
      %time.timeago{ :'data-msec' => "{{ms updated_at}}", :pubdate => "true" }
        {{time_ago updated_at}}
    %p
      {{#user}}
      %a{ :href => "{{task_list_url}}" } {{full_name}}
      {{/user}}
      created the task list
      {{#target}}
      %a{ :href => "{{task_list_url}}" } {{name}}
      {{/target}}
      %span.project
        in the project
        {{#project}}
        %a{ :href => "{{task_list_url}}" } {{name}}
        {{/project}}
