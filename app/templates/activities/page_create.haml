.activity
  {{#user}}
  <a href="{{page_url}}" class="micro_avatar" style="background: url({{micro_avatar_url}}) no-repeat"></a>
  {{/user}}
  .activity_block
    .date
      %time.timeago{ :'data-msec' => "{{ms updated_at}}", :pubdate => "true" }
        {{time_ago updated_at}}
    %p
      {{#user}}
      %a{ :href => "{{page_url}}" } {{full_name}}
      {{/user}}
      created the page
      {{#target}}
      %a{ :href => "{{page_url}}" } {{name}}
      {{/target}}
      %span.project
        in the project
        {{#project}}
        %a{ :href => "{{page_url}}" } {{name}}
        {{/project}}
