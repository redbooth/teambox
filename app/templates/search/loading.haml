%h2
  =raw t('search.results_for', :search_terms => "<strong>{{query}}</strong>")

#search_results
  %p.loading
    %img{ :src => '/images/loading.gif' }
    =t 'common.loading'

%p
  %a.closePane{ :href => '#' }=t 'search.close_search'
