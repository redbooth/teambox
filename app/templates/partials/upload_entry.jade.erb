- each file in self.files
  li(id="file_#{file.id}")
    | #{file.name} (#{plupload.formatSize(file.size)})
    div(class='progressbar yellow', style='width: 100px;display:none')
      div(class='bar1')
      div(class='bar2')
      div(class='bar3')
    a(id='#{file.id}')
      | (X)
