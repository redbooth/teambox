p <%=t('comments.watchers.explain')%>

.watcher
  a(href= '#', data-login= 'all')
    strong <%= t('comments.watchers.all_users')%>

- each user in self.users
  .watcher
    a(href= '#', data-login= user.username)= self.full_name(user)
